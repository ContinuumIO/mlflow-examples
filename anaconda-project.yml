name: mlflow-examples

channels:
  - https://conda.anaconda.org/ae5-admin
  - defaults

platforms:
  - linux-64
  - osx-64
  - osx-arm64
  - win-64

env_specs:
  default:
    description: Default Environment
    packages:
      # Language Level
      - python>=3.8

      # AE5
      - ipykernel
      - ae5-tools
